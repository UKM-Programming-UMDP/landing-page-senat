import React from "react";
import Fullpage, {
  FullPageSections,
  FullpageNavigation,
} from "@ap.cx/react-fullpage";

const fullPageScroll = () => {
    return(
        <Fullpage>
            <FullPageSections>
                
            </FullPageSections>
        </Fullpage>
    )
};

export default fullPageScroll;
